import 'package:flutter/material.dart';

/// тёмно-синяя палитра (dark theme)
class AppColors {
  AppColors._();

  static const Color background = Color(0xFF0E1621); // фон чата/экранов
  static const Color surface = Color(0xFF17212B); // карточки, поля ввода
  static const Color surfaceVariant = Color(0xFF1E2C3A); // чуть светлее surface
  static const Color primary = Color(0xFF2AABEE);
  static const Color primaryDark = Color(0xFF229ED9);
  static const Color accent = Color(0xFF5288C1);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF7D8B99);
  static const Color divider = Color(0xFF263440);
  static const Color error = Color(0xFFEB5545);
}